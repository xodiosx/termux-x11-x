<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"   <!-- ADDED THIS LINE -->
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false">

    <!-- MAIN CONTENT AREA -->
    <FrameLayout
        android:id="@+id/id_display_window"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true">

        <FrameLayout
            android:id="@+id/frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black"
            android:clipToPadding="false"
            android:padding="0dp">

            <com.termux.x11.LorieView
                android:id="@+id/lorieView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:focusableInTouchMode="true"
                android:visibility="visible" />

            <!-- STUB – shown when not connected; redesigned like the default layout -->
            <LinearLayout
                android:id="@+id/stub"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@android:color/black"
                android:gravity="center"
                android:orientation="vertical"
                android:visibility="visible">

                <ImageView
                    android:id="@+id/x11_image"
                    android:layout_width="130dp"
                    android:layout_height="130dp"
                    android:contentDescription="X Logo"
                    android:scaleType="fitCenter"
                    app:srcCompat="@drawable/ic_x11_icon"
                    tools:ignore="HardcodedText" />   <!-- now valid -->

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="20dp" />

                <TextView
                    android:id="@+id/textView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:text="@string/not_connected"
                    android:textColor="#FFFFFF"
                    android:textSize="20sp" />

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="40dp" />

                <Button
                    android:id="@+id/preferences_button"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/preferences_button_text" />

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="10dp" />

                <Button
                    android:id="@+id/exit_button"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/exit_button_exit_app" />
            </LinearLayout>
        </FrameLayout>

        <!-- Stylus auxiliary buttons (unchanged) -->
        <LinearLayout
            android:id="@+id/mouse_helper_visibility"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:translationX="0px"
            android:translationY="0px">

            <Button
                android:id="@+id/button_visibility"
                android:layout_width="64px"
                android:layout_height="64px"
                android:minWidth="44dp"
                android:minHeight="22dp"
                android:padding="0px"
                android:text="X"
                android:textSize="8sp" />

            <LinearLayout
                android:id="@+id/mouse_helper_secondary_layer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <Button
                    android:id="@+id/button_left_click"
                    android:layout_width="64px"
                    android:layout_height="64px"
                    android:minWidth="44dp"
                    android:minHeight="22dp"
                    android:padding="0px"
                    android:text="L"
                    android:textSize="8sp" />

                <Button
                    android:id="@+id/button_middle_click"
                    android:layout_width="64px"
                    android:layout_height="64px"
                    android:minWidth="44dp"
                    android:minHeight="22dp"
                    android:padding="0px"
                    android:text="M"
                    android:textSize="8sp" />

                <Button
                    android:id="@+id/button_right_click"
                    android:layout_width="64px"
                    android:layout_height="64px"
                    android:minWidth="44dp"
                    android:minHeight="22dp"
                    android:padding="0px"
                    android:text="R"
                    android:textSize="8sp" />
            </LinearLayout>
        </LinearLayout>

        <!-- Mouse auxiliary buttons (unchanged) -->
        <LinearLayout
            android:id="@+id/mouse_buttons"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center"
            android:layout_marginBottom="40dp"
            android:alpha="0.7"
            android:orientation="vertical"
            android:translationX="0px"
            android:translationY="0px">

            <LinearLayout
                android:id="@+id/mouse_buttons_secondary_layer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <Button
                    android:id="@+id/mouse_button_left_click"
                    android:layout_width="96dp"
                    android:layout_height="48dp"
                    android:background="@drawable/mouse_buttons"
                    android:minWidth="48dp"
                    android:minHeight="48dp"
                    android:padding="0px"
                    android:text="L"
                    android:textColor="@android:color/black"
                    android:textSize="8sp" />

                <Button
                    android:id="@+id/mouse_button_middle_click"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="@drawable/mouse_buttons"
                    android:minWidth="48dp"
                    android:minHeight="48dp"
                    android:padding="0px"
                    android:text="M"
                    android:textColor="@android:color/black"
                    android:textSize="8sp" />

                <Button
                    android:id="@+id/mouse_button_right_click"
                    android:layout_width="96dp"
                    android:layout_height="48dp"
                    android:background="@drawable/mouse_buttons"
                    android:minWidth="48dp"
                    android:minHeight="48dp"
                    android:padding="0px"
                    android:text="R"
                    android:textColor="@android:color/black"
                    android:textSize="8sp" />
            </LinearLayout>

            <ImageButton
                android:id="@+id/mouse_buttons_position"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="center"
                android:adjustViewBounds="true"
                android:background="@drawable/mouse_buttons"
                android:contentDescription="MOVE"
                android:minWidth="40dp"
                android:minHeight="40dp"
                android:padding="2dp"
                android:scaleType="fitCenter"
                android:src="@android:drawable/ic_menu_mylocation" />
        </LinearLayout>

        <!-- Extra keys toolbar (ID must match MainActivity) -->
        <androidx.viewpager.widget.ViewPager
            android:id="@+id/display_terminal_toolbar_view_pager"
            android:layout_width="match_parent"
            android:layout_height="37.5dp"
            android:layout_gravity="bottom|center"
            android:background="@android:color/black"
            android:visibility="gone" />

        <View
            android:id="@+id/activity_termux_bottom_space_view"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_gravity="bottom|center"
            android:background="@android:color/transparent" />
    </FrameLayout>

    <!-- DRAWER CONTENT -->
    <LinearLayout
        android:id="@+id/drawer_content"
        android:layout_width="320dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="?android:attr/colorBackground"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="vertical">

        <!-- Drawer Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#F265059A"
            android:clickable="false"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="X11 Settings"
                android:textColor="@android:color/white"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.8"
                android:text="Termux X11 Configuration"
                android:textColor="@android:color/white"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Preferences Container -->
        <FrameLayout
            android:id="@+id/preferences_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?android:attr/colorBackground"
            android:clickable="true"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:visibility="gone" />
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>