name: Build Apk to release 

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release tag (e.g., v1.2.3) – leave empty to skip release'
        required: false
        default: ''
  push:
    branches:
      - master
    tags:
      - 'v*'              # optionally trigger on version tags
    paths-ignore:
      - '**.md'
      - 'img/**'
  pull_request:
    branches:
      - master
    paths-ignore:
      - '**.md'
      - 'img/**'

permissions:
  contents: write

jobs:
  build:
    name: "Build"
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v6
      with:
        submodules: recursive
        fetch-depth: 0   # needed to get all tags for version detection

    - name: Validation
      uses: gradle/actions/wrapper-validation@v5

    - name: Java setup
      uses: actions/setup-java@v5
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Restore cache
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Build
      run: ./gradlew assembleDebug

    - name: Build companion package
      run: ./build_termux_package

    # ------------------------------------------------------------
    # Upload APKs and debs as artifacts (for later use)
    # ------------------------------------------------------------
    - name: Store app-arm64-v8a-debug
      uses: actions/upload-artifact@v5
      with:
        name: termux-x11-arm64-v8a-debug
        path: ./app/build/outputs/apk/debug/app-arm64-v8a-debug.apk

    - name: Store app-armeabi-v7a-debug
      uses: actions/upload-artifact@v5
      with:
        name: termux-x11-armeabi-v7a-debug
        path: ./app/build/outputs/apk/debug/app-armeabi-v7a-debug.apk

    - name: Store app-universal-debug
      uses: actions/upload-artifact@v5
      with:
        name: termux-x11-universal-debug
        path: ./app/build/outputs/apk/debug/app-universal-debug.apk

    - name: Store app-x86_64-debug
      uses: actions/upload-artifact@v5
      with:
        name: termux-x11-x86_64-debug
        path: ./app/build/outputs/apk/debug/app-x86_64-debug.apk

    - name: Store app-x86-debug
      uses: actions/upload-artifact@v5
      with:
        name: termux-x11-x86-debug
        path: ./app/build/outputs/apk/debug/app-x86-debug.apk

    - name: Store companion packages
      uses: actions/upload-artifact@v5
      with:
        name: termux-companion-packages
        path: |
          ./app/build/outputs/apk/debug/termux-x11-*-all.deb
          ./app/build/outputs/apk/debug/termux-x11-*-any.pkg.tar.xz

    - name: Store unstripped libraries with debug symbols
      uses: actions/upload-artifact@v5
      with:
        name: termux-x11-unstripped-libraries-for-ndk-stack
        path: ./app/build/intermediates/cxx/Debug/*

    # ------------------------------------------------------------
    # Nightly Release (only on master pushes, tag = nightly)
    # ------------------------------------------------------------
    - name: Update Nightly Release
      uses: andelf/nightly-release@main
      if: github.repository == 'xodiosx/termux-x11-xodmod' && github.ref == 'refs/heads/master'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: nightly
        name: 'Nightly Release $$'
        draft: false
        prerelease: false
        body: |
          This is a nightly release of the termux-x11 app.
          Based on ${{ github.sha }}
        files: |
          ./app/build/outputs/apk/debug/app-*-debug.apk
          ./app/build/outputs/apk/debug/termux-x11-*-all.deb
          ./app/build/outputs/apk/debug/termux-x11-*-any.pkg.tar.xz

    # ------------------------------------------------------------
    # New: Create a proper Release with a tag
    # ------------------------------------------------------------
    # Determine the release tag:
    #   - If workflow_dispatch and release_version input is provided, use that.
    #   - Else if a Git tag triggered the workflow, use that tag.
    #   - Otherwise skip.
    - name: Determine release tag
      id: get_tag
      run: |
        if [ "${{ github.event_name }}" == "workflow_dispatch" ] && [ -n "${{ github.event.inputs.release_version }}" ]; then
          echo "tag=${{ github.event.inputs.release_version }}" >> $GITHUB_OUTPUT
        elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
          echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
        else
          echo "tag=" >> $GITHUB_OUTPUT
        fi

    - name: Create GitHub Release
      if: steps.get_tag.outputs.tag != ''
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.get_tag.outputs.tag }}
        name: Release ${{ steps.get_tag.outputs.tag }}
        draft: false
        prerelease: false   # change to true if you want pre-release
        generate_release_notes: true
        fail_on_unmatched_files: false
        files: |
          ./app/build/outputs/apk/debug/app-*-debug.apk
          ./app/build/outputs/apk/debug/termux-x11-*-all.deb
          ./app/build/outputs/apk/debug/termux-x11-*-any.pkg.tar.xz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}